# Used by electron-updater in DEVELOPMENT (unpackaged `electron .`) runs only.
# The packaged app reads `app-update.yml` generated by electron-builder from
# the `publish` section of electron-builder.yml.
provider: github
owner: dupipcom
repo: iris
